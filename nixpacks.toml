# Nixpacks configuratie voor Railway
# Dit zorgt ervoor dat FFmpeg wordt geïnstalleerd

[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn api.analyze:app --host 0.0.0.0 --port $PORT"

