# FastAPI voor serverless endpoints
fastapi>=0.104.0
pydantic>=2.0.0
uvicorn[standard]>=0.24.0

# Core audio processing
# Gebruik exacte versies voor betere compatibiliteit (zoals in requirements_standalone.txt)
librosa==0.10.1
numpy==1.24.3
scipy>=1.2.0,<1.12.0

# Librosa dependencies (vereist voor audio I/O en resampling)
soundfile>=0.12.1
resampy>=0.4.2

# Metadata en bitrate extractie
mutagen==1.47.0

# Extra dependencies voor librosa (mogelijk nodig)
audioread>=3.0.0
